{
  "name": "campus-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --middlewares ./middleware.js"
  },
  "dependencies": {
    "json-server": "^0.17.1",
    "cors": "^2.8.5"
  }
}